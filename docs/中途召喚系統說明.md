# 中途召喚系統說明

## 概述

「事件疊層最淺層執行」中途召喚系統允許在遊戲進行中，於兩名相鄰玩家之間動態插入新的 AI 玩家。

## 系統架構

```
[Lua Skill]
    → room:requestSummonBetween(before, after, general_name)
    → 參數校驗失敗 → 全部中止（不回滾，直接拋錯）
    → m_pendingSummons.enqueue()
    → 事件迴圈持續執行
    → event_stack.isEmpty() (最淺層)
    → processPendingSummons()
       → insertPlayerMidGame(before, after, general_name)
       → 廣播客戶端
       → doBroadcastNotify(S_COMMAND_ADD_PLAYER_DYNAMIC, info)
       → Client::onPlayerAddedMidGame() → emit player_added()
       → RoomScene::addPlayer() [Dynamic Photo]
       → doBroadcastNotify(S_COMMAND_ARRANGE_SEATS, ...)
       → emit seats_arranged() → arrangeSeats()
```

## 協議指令

| 指令 | 用途 |
|------|------|
| S_COMMAND_ADD_PLAYER_DYNAMIC | 中途召喚新玩家（新增） |
| S_COMMAND_ARRANGE_SEATS | 客戶端重排座次（現有） |

## 伺服器端修改

### protocol.h

新增協議指令：
```cpp
S_COMMAND_UPDATE_CARD_DESC,
S_COMMAND_ADD_PLAYER_DYNAMIC,  // 中途召喚新玩家
S_COMMAND_SWITCH_CONTEXT = 128,
S_COMMAND_SYNC_PILE = 129,      // 同步牌堆（玩家私人牌堆）
```

### player-lifecycle-service.h（召喚狀態與結構）

召喚佇列狀態已移入 `src/server/player-lifecycle-service.h`（`PlayerLifecycleService`）：
```cpp
void requestSummonBetween(ServerPlayer *before, ServerPlayer *after,
                          const QString &generalName);
bool hasPendingSummons() const;
void processPendingSummons();
ServerPlayer *insertPlayerMidGame(ServerPlayer *before, ServerPlayer *after,
                                  const QString &generalName);

private:
    struct SummonRequest
    {
        ServerPlayer *before;       // 插入點前的玩家（不可為 nullptr）
        ServerPlayer *after;        // 插入點後的玩家（不可為 nullptr）
        QString generalName;        // 武將名（不可為空）
    };

    QList<SummonRequest> m_pendingSummons;
    QList<ServerPlayer *> m_dynamicPlayers;  // 追蹤中途召喚的玩家
```

### room.h

`Room` 只保留方法宣告並委派給 `PlayerLifecycleService`：
```cpp
void requestSummonBetween(ServerPlayer *before, ServerPlayer *after, const QString &general_name);
void processPendingSummons();
bool hasPendingSummons() const;
```

### room.cpp

`Room` 層只剩轉發實作（`src/server/room.cpp`）：
```cpp
bool Room::hasPendingSummons() const
{
	return m_playerLifecycle->hasPendingSummons();
}

void Room::requestSummonBetween(ServerPlayer *before, ServerPlayer *after,
                                const QString &general_name)
{
	m_playerLifecycle->requestSummonBetween(before, after, general_name);
}

void Room::processPendingSummons()
{
	m_playerLifecycle->processPendingSummons();
}
```

### player-lifecycle-service.cpp

主要方法實現（`src/server/player-lifecycle-service.cpp`）：
- `hasPendingSummons()` - 檢查是否有待處理的中途召喚請求
- `requestSummonBetween()` - 將召喚請求加入佇列
- `processPendingSummons()` - 處理所有待處理的召喚請求
- `insertPlayerMidGame()` - 執行實際的玩家插入邏輯

`insertPlayerMidGame()` 流程：
1. 參數校驗（before、after 不能為 nullptr，general_name 不能為空）
2. 校驗 before 和 after 是否相鄰
3. 創建新的 ServerPlayer（AI 機器人）
4. 設定玩家屬性（objectName、general、hp、state）
5. 重構環狀鏈結（在 before 和 after 之間插入新玩家）
6. 更新玩家列表與存活列表（roster 插入）
7. 廣播 S_COMMAND_ADD_PLAYER_DYNAMIC 通知客戶端
8. 重新分配座次並廣播
9. 廣播 S_COMMAND_ARRANGE_SEATS
10. 註冊觸發技能
11. 發初始手牌（4 張）
12. 召喚成功的玩家由 `processPendingSummons()` 加入 `m_dynamicPlayers`

### roomthread.cpp

處理點已封裝進 `RoomThread::flushOutermostDeferredWork()`（`src/server/roomthread.cpp`），在事件堆疊清空時呼叫：
```cpp
void RoomThread::flushOutermostDeferredWork(Room *room)
{
	if (!room || !event_stack.isEmpty()) return;

	if (m_playerUiStateDirty) {
		// PlayerUIState owns the existing server-to-client UI notification path.
		m_playerUiStateDirty = false;
		foreach (ServerPlayer *player, room->getAlivePlayers())
			player->refreshUIState();
	}

	refreshDistanceCacheIfDirty(room);

	if (room->hasPendingSummons())
		room->processPendingSummons();

	room->processPendingAnytimeSkills();
}
```

### marshal() 擴展

`marshal()` 已移入 `PlayerLifecycleService::marshal()`（`src/server/player-lifecycle-service.cpp`），為斷線重連的玩家發送中途召喚玩家的信息（QVariantMap payload，screen_name 不做 base64）：
```cpp
foreach (ServerPlayer *dynamicPlayer, m_dynamicPlayers) {
    QVariantMap info{{QStringLiteral("schema_version"), 1},
                     {QStringLiteral("player_name"), dynamicPlayer->objectName()},
                     {QStringLiteral("screen_name"), dynamicPlayer->screenName()},
                     {QStringLiteral("avatar"), dynamicPlayer->getGeneralName()}};
    m_notifier.doNotify(player, S_COMMAND_ADD_PLAYER_DYNAMIC, info);
}
```

## SWIG 接口導出

```cpp
class Room: public QThread {
public:
    // ... existing methods ...
    void requestSummonBetween(ServerPlayer* before, ServerPlayer* after, const char* general_name);
};
```

## 客戶端修改

### client.cpp

新增回調注册：
```cpp
m_callbacks[S_COMMAND_ADD_PLAYER_DYNAMIC] = &Client::onPlayerAddedMidGame;
```

新增回調實現：
```cpp
void Client::onPlayerAddedMidGame(const QVariant &player_info)
{
    const QVariantMap info = player_info.toMap();
    QString name = info.value(QStringLiteral("player_name")).toString();
    QString screen_name = info.value(QStringLiteral("screen_name")).toString();
    QString avatar = info.value(QStringLiteral("avatar")).toString();

    if (getPlayer(name)) return;  // 避免重複

    ClientPlayer *player = new ClientPlayer(this);
    player->setObjectName(name);
    player->setScreenName(screen_name);
    player->setProperty("avatar", avatar);
    player->setSeat(m_players.length() + 1);

    m_players << player;
    emit player_added(player);
}
```

### roomscene.cpp

`addPlayer()` 修改為支援動態創建 Photo：
```cpp
void RoomScene::addPlayer(ClientPlayer* player)
{
    // 1. 嘗試填補空位
    for (int i = 0; i < photos.length(); i++) {
        if (photos[i]->getPlayer() == nullptr) {
            photos[i]->setPlayer(player);
            name2photo[player->objectName()] = photos[i];
            if (!Self->hasFlag("marshalling"))
                Sanguosha->playSystemAudioEffect("add-player", false);
            return;
        }
    }

    // 2. 動態創建新 Photo
    Photo* photo = new Photo;
    photo->setZValue(1);
    addItem(photo);

    photo->setPlayer(player);
    photos << photo;
    name2photo[player->objectName()] = photo;

    // 3. 連接信號
    item2player.insert(photo, player);
    connect(photo, SIGNAL(selected_changed()), this, SLOT(updateSelectedTargets()));
    connect(photo, SIGNAL(enable_changed()), this, SLOT(onEnabledChange()));

    if (!Self->hasFlag("marshalling"))
        Sanguosha->playSystemAudioEffect("add-player", false);
}
```

`updateTable()` 邊界保護：
```cpp
static const int s_regularSeatIndex[][20] = {
    { 1 },
    { 5, 6 },
    { 5, 1, 6 },
    // ...（依照片數列出各人數的版面配置，共 1~19 人）
    { 3, 3, 3, 3, 3, 7, 7, 7, 7, 7, 7, 7, 7, 7, 7, 4, 4, 4, 4, 4 }
};

const int *seatToRegion = s_regularSeatIndex[photos.length()-1];
```

`arrangeSeats()` 信號維護：
```cpp
// set item to player mapping
if (item2player.isEmpty()) {
    // ... 現有初始化 ...
} else {
    foreach (Photo* photo, photos) {
        if (!item2player.contains(photo)) {
            item2player.insert(photo, photo->getPlayer());
            connect(photo, SIGNAL(selected_changed()), this, SLOT(updateSelectedTargets()));
            connect(photo, SIGNAL(enable_changed()), this, SLOT(onEnabledChange()));
        }
    }
}
```

## Lua 調用範例

```lua
-- 在 player1 和 player2 之間插入武將「張遼」
room:requestSummonBetween(player1, player2, "zhangliao")
```

## 斷線重連處理

當玩家在遊戲中斷線並重連時：
1. `reconnect()` 調用 `marshal()` 向重連玩家發送完整的房間狀態
2. `marshal()` 會檢查 `m_dynamicPlayers` 清單
3. 為每個中途召喚的玩家發送 `S_COMMAND_ADD_PLAYER_DYNAMIC`
4. 客戶端收到後創建對應的 Photo，確保視圖同步

## 實現順序

| Phase | 檔案 | 內容 |
|-------|------|------|
| 1 | protocol.h | 新增 S_COMMAND_ADD_PLAYER_DYNAMIC |
| 2 | player-lifecycle-service.h | SummonRequest 結構與召喚方法宣告（room.h 僅留轉發宣告） |
| 3 | player-lifecycle-service.cpp | 實現 requestSummonBetween, processPendingSummons, insertPlayerMidGame（room.cpp 轉發） |
| 4 | roomthread.cpp | 在 flushOutermostDeferredWork() 的 event_stack.isEmpty() 後呼叫 processPendingSummons() |
| 5 | swig/sanguosha.i | 導出 requestSummonBetween 給 Lua |
| 6 | client.cpp | 新增 onPlayerAddedMidGame 回調與註冊 |
| 7 | roomscene.cpp | 修改 addPlayer() 支援動態 Photo |
