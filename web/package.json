{
  "name": "qsanguosha-web",
  "private": true,
  "type": "module",
  "scripts": {
    "check:protocol": "node scripts/check-protocol-sync.mjs",
    "check:translations": "node scripts/check-translations.mjs",
    "check:seats": "node scripts/check-seat-ring-sync.mjs",
    "dev": "node scripts/check-translations.mjs && vite",
    "build": "node scripts/check-protocol-sync.mjs && node scripts/check-translations.mjs && node scripts/check-seat-ring-sync.mjs && tsc --noEmit && vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  }
}
