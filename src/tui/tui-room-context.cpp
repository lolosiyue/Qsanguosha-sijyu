#include "tui-room-context.h"

// Implementation lives in runtime/client-room-context.h. Keep this translation
// unit so the TUI source inventory and downstream project files remain stable
// during the client-runtime migration.
